extends layout

block content

    h1 Id: #{title}

    p #[strong Title:] 
        a(href=book.url) #{book.title}

    p #[strong Imprint:] #{bookinstance.imprint}

    p #[strong Status:]&nbsp;
        if bookinstance.status == 'Available'
            text.text-success #{bookinstance.status}
        else if bookinstance.status == 'Maintenance'
            text.text-danger #{bookinstance.status}
        else
            text.text-warning #{bookinstance.status}

    if bookinstance.status != 'Available'
        p #[strong Due back:] #{bookinstance.due_back_formatted}

    hr
    p
        a(href=bookinstance.url+'/delete') Delete Bookinstance
    p
        a(href=bookinstance.url+'/update') Update Bookinstance